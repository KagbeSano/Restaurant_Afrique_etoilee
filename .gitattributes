# For text files, normalize line endings to LF in the repo,
# convert to CRLF when checked out on Windows
* text=auto

# Optional: Force specific file types to use LF
*.sh text eol=lf
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.json text eol=lf
*.md text eol=lf

# Optional: Keep Windows files in CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

